{
  "name": "eleventy-plugin-mastodon-share-demo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "eleventy --input=src --output=dist --serve",
    "build": "eleventy --input=src --output=dist"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.1.2"
  }
}